# Codex Notes

- Do not use the user's reserved note marker in comments. It is their personal placeholder marker.
- Keep changes tightly scoped to the explicit request.
- For this site, prefer preserving the existing static HTML/CSS/JS structure unless the user asks for a larger refactor.
